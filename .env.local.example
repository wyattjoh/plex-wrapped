# App URL (used for OAuth callback)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Enable debug logging for wrapped stats calculation
# Set to "true" to see detailed logs in the server console
DEBUG_WRAPPED=false
